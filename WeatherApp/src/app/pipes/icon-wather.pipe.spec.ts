import { IconWatherPipe } from './icon-wather.pipe';

describe('IconWatherPipe', () => {
  it('create an instance', () => {
    const pipe = new IconWatherPipe();
    expect(pipe).toBeTruthy();
  });
});
